{
  "name": "cleanquote-public-storefront",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/order.js --bundle --format=iife --platform=browser --target=es2022 --minify --legal-comments=none --outfile=docs/assets/order.js",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "nostr-tools": "2.24.1"
  },
  "devDependencies": {
    "esbuild": "0.25.8"
  }
}
